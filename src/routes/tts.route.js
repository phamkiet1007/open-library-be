// const express = require("express");
// const router = express.Router();
// const { authenticate, optionalAuth } = require('../middlewares/auth.middleware');
// const { isAdmin, isMember } = require('../middlewares/role.middleware');

// const { generatePreviewAudioFromPdf } = require("../services/tts.service");


// router.post("/audio-preview/:bookId", authenticate, isAdmin, generatePreviewAudioFromPdf);

// module.exports = router;
